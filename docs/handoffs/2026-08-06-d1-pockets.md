# D1 — the §8.6 pocket system

**Branch** `feat/lexicon-and-components` · **tip** `a047db7` · **Core 2045 / Web 747, 0 failing**
Four commits, all pushed. Working tree holds only the other writer's files.

## Done

All ten of §8.6's tactical pocket items are built. Five already existed; the five this packet added:

| Item | Rule site | Ruling |
|---|---|---|
| Greased Feather | `Displacement.Preview`/`Resolve` request composition; `Consumables.GreaseBonus` | D-189, D-190 |
| Chalk Mark | writes `Unit.RattledFor` — Rattling Impact's own field | D-190 |
| Thorn Pouch | `Consumables.Scatter` / `FadeTemporaryTerrain`, `GameState.TemporaryTerrain` | D-191 |
| Split Reed | `Consumables.Offer` + `Game.ApplySplitReed` / `SplitReedSwapIsLegal` | D-192 |
| Signal Whistle | `Consumables.Whistle` — swaps two entries in `state.Units` | D-193 |

Plus **D-194**: with no replace/drop surface, a full pocket is offered no one-shot at all.

**Old Rope's doomed-cling clause was already built** (`Pits.AnyRope`, D-127) — the brief's premise
was stale there. Verified, not rewritten.

## Not done — the exact next step

**Deep Pockets (§8.6's second pocket) is the one piece of the D1 brief left.** It was deliberately
not started rather than half-started, because it is a shape change and not a card:

1. `DuckLoadout.Pocket` (one `Consumable?`) becomes a collection plus a capacity read off
   `Unlocks` — `Unlock.DeepPockets` does not exist yet and is documented as absent in `Unlock.cs`.
2. **`UseConsumableCommand` must then name which pocket.** Its XML doc currently argues it needs no
   item name *because a duck has one pocket*; with two that argument fails. That is a command-shape
   change, so `RunRecord.Format` **and** `ParseCommand` both need it, or logs stop replaying —
   `CombatLogTests.EveryCommandType_IsKnownToTheCommandLog` is the gate.
3. Callers to sweep: `Consumables.TimingAllows`/`Legal`/`Use`, `Pits.AnyRope`
   (`Loadout.Pocket == Consumable.OldRope` must become "any pocket holds one"),
   `CampCatalogue.EligibleFor` (the D-194 filter becomes "has a free pocket"),
   `Camp.Apply`, `RunSave.ParseLoadout` (see the review queue — it has no capacity guard today),
   `PocketSlots.PocketsOf` (already returns an array, and already documents the day this lands),
   `CampCards.ReasonFor` (the D-194 reason line must become capacity-aware).

## Traps

- **`state.Units` order is now load-bearing** (D-193). Do not re-sort it.
- The pinned-count tests move together every time a one-shot lands:
  `ConsumableTests.TheTacticalPool_IsTheTenThatAreBuilt`,
  `CampTests.TheCatalogueHoldsTheWholeV1Pool_AndNothingBeyondIt`,
  `ConsumableDefinitionTests.TheAimKinds_AreTheFiveThatAreBuilt`.
- `ConsumableDefinition`'s registry order **must** equal `CampCatalogue.ConsumablePool()` order —
  append to both, never reorder.
- `StateEqualityCoverageTests` fails loudly on any new `GameState` property. It caught
  `TemporaryTerrain` before I did; trust it.
- A consumable is **unreachable at the first camp** for every seed 1–40 (review queue).
