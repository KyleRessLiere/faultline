# Handoff — <what this session was about>

**Date · Branch · Tree state**

> `YYYY-MM-DD` · `branch-name` · N tests green / **RED — see below** · X uncommitted files

One paragraph: what this session set out to do, and whether it got there.

---

## Exact next step

The single most important section. A command, an edit, or a decision — not an area.

> Example: *Add `int Verve` to `Unit`, defaulted 0, then the four charge listeners. `Combat.ApplyDamage`
> is the seam for three of them; the Wardbearer's needs `Guard.Mitigate`. Do NOT start the spenders
> until the meter has tests.*

If the next step is blocked on a decision, state the decision and the options, and say which you would
pick and why.

## State of the work

| Piece | State | Where |
|---|---|---|
| | done / half / not started | file or branch |

**Uncommitted:** what is dirty and why it was not committed.

**Green?** If red, exactly which tests and whether it is expected.

## What is running in parallel

Only if something is. Name the agent, the files it owns, and what will collide.

> Nothing is running. — is a perfectly good entry, and worth writing rather than omitting.

## Decided, and deliberately not done

Rulings this session made, linked to `DECISIONS.md` by number. And the things that were considered
and *rejected* — so the next session does not re-open them.

| Decision | Where | Why it matters here |
|---|---|---|

## Traps

The ones that actually bit, with evidence. Not general advice.

## Open questions

Things nobody has decided, distinguished from things decided and disliked. Say who can answer.
