diff --git a/src/Faultline.Core/Campaign/Map/MapRestNodeHandler.cs b/src/Faultline.Core/Campaign/Map/MapRestNodeHandler.cs
index d52b94a..ff92670 100644
--- a/src/Faultline.Core/Campaign/Map/MapRestNodeHandler.cs
+++ b/src/Faultline.Core/Campaign/Map/MapRestNodeHandler.cs
@@ -25,12 +25,12 @@ namespace Faultline.Core
         /// </summary>
         /// <remarks>
         /// Kept as the handler's name for the half-formula because GAMEPLAY.md and the tests both
-        /// point at it; the arithmetic itself belongs to <see cref="StillPond.HalfOf"/>, which the
+        /// point at it; the arithmetic itself belongs to <see cref="StillPond.QuarterOf"/>, which the
         /// pre-boss floor and the screen also read.
         /// </remarks>
         /// <param name="maxHp">The duck's ceiling, raises included.</param>
         /// <returns>Hit points restored.</returns>
-        public static int HealFor(int maxHp) => StillPond.HalfOf(maxHp);
+        public static int HealFor(int maxHp) => StillPond.QuarterOf(maxHp);
 
         /// <inheritdoc/>
         public override RunState Enter(RunState state, CampaignNode node, RunContext context) =>
diff --git a/src/Faultline.Core/Campaign/Map/StillPond.cs b/src/Faultline.Core/Campaign/Map/StillPond.cs
index 16638f1..cb4902c 100644
--- a/src/Faultline.Core/Campaign/Map/StillPond.cs
+++ b/src/Faultline.Core/Campaign/Map/StillPond.cs
@@ -32,16 +32,20 @@ namespace Faultline.Core
     public static class StillPond
     {
         /// <summary>
-        /// Half a duck's ceiling, rounded up — what a mid-act pond gives back per duck, off that
-        /// duck's own maximum, so a raised ceiling heals more (MASTER_DESIGN §8.5, "heal ~half").
+        /// A quarter of a duck's ceiling, rounded up — what a mid-act pond gives back per duck, off
+        /// that duck's own maximum, so a raised ceiling heals more (MASTER_DESIGN §8.5).
         /// </summary>
         /// <remarks>
-        /// Integer arithmetic, rounded up, so odd ceilings do not quietly heal less than half. A
-        /// 7-max duck gets 4, not 3.
+        /// <b>Half until the Ratio Pass (§3, locked ab).</b> It moved because the pool it heals from
+        /// tripled: half of 42 is a whole Wardbearer back at every pond, which makes a wound
+        /// something you walk off rather than something you carry across four nodes — the exact
+        /// attrition the Ratio Pass exists to create. The rule is a fraction of the duck's own
+        /// ceiling, not a flat number, so it still moves on its own the next time HP does.
+        /// Integer arithmetic, rounded up, so odd ceilings do not quietly heal less than the share.
         /// </remarks>
         /// <param name="maxHp">The duck's ceiling, raises included.</param>
         /// <returns>Hit points restored.</returns>
-        public static int HalfOf(int maxHp) => maxHp <= 0 ? 0 : (maxHp + 1) / 2;
+        public static int QuarterOf(int maxHp) => maxHp <= 0 ? 0 : (maxHp + 3) / 4;
 
         /// <summary>
         /// What a duck stands up on after resting at a pond of this depth — an absolute, not a
@@ -69,7 +73,7 @@ namespace Faultline.Core
                 return unit.Hp;
             }
 
-            int to = depth == PondDepth.PreBoss ? unit.MaxHp : unit.Hp + HalfOf(unit.MaxHp);
+            int to = depth == PondDepth.PreBoss ? unit.MaxHp : unit.Hp + QuarterOf(unit.MaxHp);
             return to > unit.MaxHp ? unit.MaxHp : to;
         }
 
diff --git a/src/Faultline.Core/Displacement/Displacement.cs b/src/Faultline.Core/Displacement/Displacement.cs
index a445c21..5ab0ee7 100644
--- a/src/Faultline.Core/Displacement/Displacement.cs
+++ b/src/Faultline.Core/Displacement/Displacement.cs
@@ -11,7 +11,16 @@ namespace Faultline.Core
     public static class Displacement
     {
         /// <summary>Damage each party takes from a collision between two bodies.</summary>
-        public const int CollisionDamage = 4;
+        /// <remarks>
+        /// <b>4 until the Ratio Pass (§3, locked ab), and it moved because enemy HP did.</b> §6's
+        /// double-kill teach — a bare collision ends a Husk outright — is the reason the board reads
+        /// as an answer rather than a slower sword, and at Husk 6 a 4 would have quietly killed it.
+        /// 6 is not a new number here: brambles, spikes and
+        /// <see cref="StructureCollisionDamage"/> already deal it, and §2 states the thesis as
+        /// "collision 6 vs attack 2". Attack damage deliberately stayed at 2, which widens the
+        /// board-over-sword gap from 2× to 3× — the thesis stated louder rather than restated.
+        /// </remarks>
+        public const int CollisionDamage = 6;
 
         /// <summary>
         /// Damage a <see cref="Structure"/> takes from a collision — more than a body takes, because
diff --git a/src/Faultline.Core/Units/UnitTemplate.cs b/src/Faultline.Core/Units/UnitTemplate.cs
index 51a551d..e2ce7be 100644
--- a/src/Faultline.Core/Units/UnitTemplate.cs
+++ b/src/Faultline.Core/Units/UnitTemplate.cs
@@ -171,42 +171,42 @@ namespace Faultline.Core
                 // push" the default, which blunts the board. Scenarios grant it instead, through the
                 // 'footing:' key in the .fight file.
                 // The Vanguard's basic shoves; the Threadcaster's may pull instead of hurting.
-                new UnitTemplate(UnitKind.Vanguard, "Vanguard", 14, 3, AttackKind.Melee, 1, 2, 0, AttackPush: 1),
-                new UnitTemplate(UnitKind.Archer, "Archer", 8, 3, AttackKind.Ranged, 3, 4, 0, MinRange: 2),
-                new UnitTemplate(UnitKind.Threadcaster, "Threadcaster", 8, 3, AttackKind.Ranged, 3, 2, 0, BasicPull: 1),
+                new UnitTemplate(UnitKind.Vanguard, "Vanguard", 42, 3, AttackKind.Melee, 1, 2, 0, AttackPush: 1),
+                new UnitTemplate(UnitKind.Archer, "Archer", 24, 3, AttackKind.Ranged, 3, 4, 0, MinRange: 2),
+                new UnitTemplate(UnitKind.Threadcaster, "Threadcaster", 24, 3, AttackKind.Ranged, 3, 2, 0, BasicPull: 1),
                 // D-058: the Wardbearer's hold aura is gone and it is heavier instead — 7 HP behind
                 // push resistance 2, the Colossus's number on a player class. The aura mechanic
                 // itself survives on the enemy Bulwark below.
-                new UnitTemplate(UnitKind.Wardbearer, "Wardbearer", 14, 3, AttackKind.Melee, 1, 2, 0, PushResistance: 2),
+                new UnitTemplate(UnitKind.Wardbearer, "Wardbearer", 42, 3, AttackKind.Melee, 1, 2, 0, PushResistance: 2),
 
                 // Brief §2: Enemies. Grappler and Stalker deal no damage at all — their whole action
                 // is the displacement their priority list calls for (Pull 2 at range 3; Push 1 in melee).
-                new UnitTemplate(UnitKind.Husk, "Husk", 4, 3, AttackKind.Melee, 1, 2, 0, Plan: EnemyPlan.Melee, Tramples: true),
-                new UnitTemplate(UnitKind.Lobber, "Lobber", 6, 2, AttackKind.Ranged, 3, 2, 0, Plan: EnemyPlan.Lobber),
-                new UnitTemplate(UnitKind.Anchor, "Anchor", 12, 1, AttackKind.Melee, 1, 4, 0, Plan: EnemyPlan.Melee, PushResistance: 1),
-                new UnitTemplate(UnitKind.Grappler, "Grappler", 10, 3, AttackKind.None, 3, 0, 0, BasicPull: 2, Plan: EnemyPlan.Grappler),
-                new UnitTemplate(UnitKind.Stalker, "Stalker", 8, 4, AttackKind.None, 1, 0, 0, BasicPush: 1, Plan: EnemyPlan.Stalker, HazardRanks: 3),
+                new UnitTemplate(UnitKind.Husk, "Husk", 6, 3, AttackKind.Melee, 1, 2, 0, Plan: EnemyPlan.Melee, Tramples: true),
+                new UnitTemplate(UnitKind.Lobber, "Lobber", 8, 2, AttackKind.Ranged, 3, 2, 0, Plan: EnemyPlan.Lobber),
+                new UnitTemplate(UnitKind.Anchor, "Anchor", 18, 1, AttackKind.Melee, 1, 4, 0, Plan: EnemyPlan.Melee, PushResistance: 1),
+                new UnitTemplate(UnitKind.Grappler, "Grappler", 14, 3, AttackKind.None, 3, 0, 0, BasicPull: 2, Plan: EnemyPlan.Grappler),
+                new UnitTemplate(UnitKind.Stalker, "Stalker", 12, 4, AttackKind.None, 1, 0, 0, BasicPush: 1, Plan: EnemyPlan.Stalker, HazardRanks: 3),
 
                 // docs/ENEMY_ROSTER.md: behaviour variants. Each one names a plan the shipped five do
                 // not have; everything else about it is a number on this row.
-                new UnitTemplate(UnitKind.Warden, "Warden", 12, 0, AttackKind.Melee, 1, 4, 2, Plan: EnemyPlan.Warden),
-                new UnitTemplate(UnitKind.Perch, "Perch", 6, 2, AttackKind.Ranged, 3, 2, 0, Plan: EnemyPlan.Perch),
-                new UnitTemplate(UnitKind.Bulwark, "Bulwark", 10, 2, AttackKind.Melee, 1, 2, 0, Plan: EnemyPlan.Melee, HoldAura: true),
-                new UnitTemplate(UnitKind.Harrier, "Harrier", 8, 4, AttackKind.None, 1, 0, 0, BasicPush: 1, Plan: EnemyPlan.Harrier),
+                new UnitTemplate(UnitKind.Warden, "Warden", 18, 0, AttackKind.Melee, 1, 4, 2, Plan: EnemyPlan.Warden),
+                new UnitTemplate(UnitKind.Perch, "Perch", 8, 2, AttackKind.Ranged, 3, 2, 0, Plan: EnemyPlan.Perch),
+                new UnitTemplate(UnitKind.Bulwark, "Bulwark", 14, 2, AttackKind.Melee, 1, 2, 0, Plan: EnemyPlan.Melee, HoldAura: true),
+                new UnitTemplate(UnitKind.Harrier, "Harrier", 12, 4, AttackKind.None, 1, 0, 0, BasicPush: 1, Plan: EnemyPlan.Harrier),
                 new UnitTemplate(UnitKind.Runt, "Runt", 2, 4, AttackKind.Melee, 1, 2, 0, Plan: EnemyPlan.Melee),
-                new UnitTemplate(UnitKind.Colossus, "Colossus", 20, 1, AttackKind.Melee, 1, 6, 0, Plan: EnemyPlan.Melee, PushResistance: 2),
+                new UnitTemplate(UnitKind.Colossus, "Colossus", 30, 1, AttackKind.Melee, 1, 6, 0, Plan: EnemyPlan.Melee, PushResistance: 2),
 
                 // docs/ENEMY_ROSTER.md: balance variants. Same plan as the archetype they vary, so the
                 // priority list is shared rather than copied — only the numbers differ.
-                new UnitTemplate(UnitKind.LesserGrappler, "Lesser Grappler", 10, 3, AttackKind.None, 2, 0, 0, BasicPull: 2, Plan: EnemyPlan.Grappler),
-                new UnitTemplate(UnitKind.BluntedStalker, "Blunted Stalker", 8, 4, AttackKind.None, 1, 0, 0, BasicPush: 1, Plan: EnemyPlan.Stalker, HazardRanks: 2),
-                new UnitTemplate(UnitKind.HeavyHusk, "Heavy Husk", 6, 3, AttackKind.Melee, 1, 2, 0, Plan: EnemyPlan.Melee),
+                new UnitTemplate(UnitKind.LesserGrappler, "Lesser Grappler", 14, 3, AttackKind.None, 2, 0, 0, BasicPull: 2, Plan: EnemyPlan.Grappler),
+                new UnitTemplate(UnitKind.BluntedStalker, "Blunted Stalker", 12, 4, AttackKind.None, 1, 0, 0, BasicPush: 1, Plan: EnemyPlan.Stalker, HazardRanks: 2),
+                new UnitTemplate(UnitKind.HeavyHusk, "Heavy Husk", 8, 3, AttackKind.Melee, 1, 2, 0, Plan: EnemyPlan.Melee),
 
                 // The reserved Footing-2 fixture. A Husk in every other number, fielded by nothing:
                 // it exists so the stacked-Footing rules have something to be asserted against
                 // without a shipped board being re-tiered behind a rules change (the D-092 trap).
-                new UnitTemplate(UnitKind.BracedHusk, "Braced Husk", 4, 3, AttackKind.Melee, 1, 2, 2, Plan: EnemyPlan.Melee),
-                new UnitTemplate(UnitKind.MobileAnchor, "Mobile Anchor", 12, 2, AttackKind.Melee, 1, 4, 0, Plan: EnemyPlan.Melee, PushResistance: 1),
+                new UnitTemplate(UnitKind.BracedHusk, "Braced Husk", 6, 3, AttackKind.Melee, 1, 2, 2, Plan: EnemyPlan.Melee),
+                new UnitTemplate(UnitKind.MobileAnchor, "Mobile Anchor", 18, 2, AttackKind.Melee, 1, 4, 0, Plan: EnemyPlan.Melee, PushResistance: 1),
 
                 // The neutral. A stat block with nothing on it — no attack, no shove, no resistance —
                 // whose whole identity is the plan it names. Fielded by nothing: it is the proof that
@@ -219,7 +219,7 @@ namespace Faultline.Core
                 // number and differs only in the list it runs. The Quarry King's three carry the
                 // boss's whole anti-displacement budget: three refusals, spent one per instance like
                 // everybody else's since D-143 retired the negating token.
-                new UnitTemplate(UnitKind.Raider, "Raider", 4, 3, AttackKind.Melee, 1, 2, 0, Plan: EnemyPlan.Raider),
+                new UnitTemplate(UnitKind.Raider, "Raider", 6, 3, AttackKind.Melee, 1, 2, 0, Plan: EnemyPlan.Raider),
                 new UnitTemplate(
                     UnitKind.QuarryKing, "Quarry King", 28, 1, AttackKind.Melee, 1, 6, 3,
                     AttackPush: 1, Plan: EnemyPlan.QuarryKing,
diff --git a/tests/Faultline.Core.Tests/DisplacementTests.cs b/tests/Faultline.Core.Tests/DisplacementTests.cs
index 1a3dae5..a1710c6 100644
--- a/tests/Faultline.Core.Tests/DisplacementTests.cs
+++ b/tests/Faultline.Core.Tests/DisplacementTests.cs
@@ -742,4 +742,43 @@ public class DisplacementTests
         Assert.Equal(2, shove.Distance);
     }
 
+    /// <summary>
+    /// <b>§6's double-kill teach: a bare collision ends a Husk outright.</b> This is the reason unit
+    /// collision moved from 4 to 6 in the Ratio Pass — enemy HP went to 1.5x and a Husk reached 6,
+    /// so a 4 would have quietly killed the teach that makes the board read as an answer rather than
+    /// a slower sword. Pinned from the two rules, never from the literals, so the next time either
+    /// number moves this fails instead of going stale.
+    /// </summary>
+    [Fact]
+    public void ABareCollision_StillEndsAHuskOutright_WhichIsWhyCollisionMovedWithEnemyHp()
+    {
+        int husk = UnitTemplate.For(UnitKind.Husk).MaxHp;
+
+        Assert.True(
+            Displacement.CollisionDamage >= husk,
+            $"collision {Displacement.CollisionDamage} no longer ends a Husk at {husk} - "
+            + "the double-kill teach is dead");
+
+        // And the gap the Ratio Pass widened on purpose: the board is worth 3x the sword, not 2x.
+        Assert.Equal(2, UnitTemplate.For(UnitKind.Vanguard).Damage);
+        Assert.Equal(3, Displacement.CollisionDamage / UnitTemplate.For(UnitKind.Vanguard).Damage);
+    }
+
+    /// <summary>
+    /// <b>The 24-vs-42 spread is deliberate and must not be flattened.</b> A flat pool was considered
+    /// and rejected: the spread is what makes the Archer's minimum range a real tax and the
+    /// Wardbearer a rock in the stream (§3, locked ab).
+    /// </summary>
+    [Fact]
+    public void ThePlayerHpSpread_IsThreeToOneAgainstTheSquishies_NotAFlatPool()
+    {
+        Assert.Equal(24, UnitTemplate.For(UnitKind.Archer).MaxHp);
+        Assert.Equal(24, UnitTemplate.For(UnitKind.Threadcaster).MaxHp);
+        Assert.Equal(42, UnitTemplate.For(UnitKind.Vanguard).MaxHp);
+        Assert.Equal(42, UnitTemplate.For(UnitKind.Wardbearer).MaxHp);
+
+        // An Archer survives three collisions and dies to the fourth; at 8 she died to two.
+        Assert.True(24 > Displacement.CollisionDamage * 3);
+        Assert.True(24 <= Displacement.CollisionDamage * 4);
+    }
 }
diff --git a/tests/Faultline.Core.Tests/MapRestAndEventTests.cs b/tests/Faultline.Core.Tests/MapRestAndEventTests.cs
index 8fe2399..bd565f5 100644
--- a/tests/Faultline.Core.Tests/MapRestAndEventTests.cs
+++ b/tests/Faultline.Core.Tests/MapRestAndEventTests.cs
@@ -12,13 +12,17 @@ public class MapRestAndEventTests
 {
     // --- The campfire ------------------------------------------------------------------------------
 
+    // A quarter since the Ratio Pass (§3, locked ab) - it moved because the pool it heals from
+    // tripled, and half of 42 is a whole Wardbearer back at every pond. Rounded up, so an odd
+    // ceiling never quietly heals less than its share.
     [Theory]
-    [InlineData(14, 7)]
-    [InlineData(16, 8)]
-    [InlineData(8, 4)]
-    [InlineData(7, 4)]
+    [InlineData(42, 11)]
+    [InlineData(24, 6)]
+    [InlineData(16, 4)]
+    [InlineData(8, 2)]
+    [InlineData(7, 2)]
     [InlineData(1, 1)]
-    public void HealFor_IsHalfTheCeilingRoundedUp(int maxHp, int expected)
+    public void HealFor_IsAQuarterOfTheCeilingRoundedUp(int maxHp, int expected)
     {
         Assert.Equal(expected, MapRestNodeHandler.HealFor(maxHp));
     }
@@ -37,7 +41,7 @@ public class MapRestAndEventTests
     }
 
     [Fact]
-    public void TheCampfire_HealsHalfEachDucksOwnCeilingRoundedUp()
+    public void TheCampfire_HealsAShareOfEachDucksOwnCeilingRoundedUp()
     {
         var run = AtTheCampfire();
         run = HurtEveryone(run, 1);
@@ -51,11 +55,14 @@ public class MapRestAndEventTests
             Assert.Equal(expected, unit.Hp);
         }
 
-        // Per duck, off its own ceiling: the Archer's 8 heals 4 and the Vanguard's 14 heals 7.
+        // Per duck, off its own ceiling, read from the rule rather than retyped: the same pond gives
+        // the Archer and the Vanguard different numbers, and that is the whole point of the share
+        // being a fraction of the duck rather than a flat heal.
         var archer = step.NewState.Squad.Single(u => u.Kind == UnitKind.Archer);
         var vanguard = step.NewState.Squad.Single(u => u.Kind == UnitKind.Vanguard);
-        Assert.Equal(5, archer.Hp);
-        Assert.Equal(8, vanguard.Hp);
+        Assert.Equal(1 + MapRestNodeHandler.HealFor(archer.MaxHp), archer.Hp);
+        Assert.Equal(1 + MapRestNodeHandler.HealFor(vanguard.MaxHp), vanguard.Hp);
+        Assert.True(vanguard.Hp > archer.Hp, "the bigger ceiling must heal the bigger share");
     }
 
     [Fact]
