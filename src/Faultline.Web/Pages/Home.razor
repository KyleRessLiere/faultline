@page "/play"
@layout MainLayout

@* The board screen is a component, not a page: the routing lives here and everything else lives in
   Shell/Playtest, where it can be split into panels that each read the session for themselves. *@

<PageTitle>PLUCK</PageTitle>

<PlaytestScreen />
